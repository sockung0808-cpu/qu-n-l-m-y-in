import { observeNotification } from '../Notification.js';
import { Observable, operate } from '../Observable.js';
export function dematerialize() {
    return (source) => new Observable((destination) => {
        source.subscribe(operate({ destination, next: (notification) => observeNotification(notification, destination) }));
    });
}//# sourceMappingURL=http://go/sourcemap/sourcemaps/3a67af7b780e0bfc8d32aefa96b8ff1cb8817f80/node_modules/rxjs/dist/esm/internal/operators/dematerialize.js.map