import { SEMANTIC_ATTRIBUTE_SENTRY_ORIGIN } from '@sentry/core';

/** Adds an origin to an OTEL Span. */
function addOriginToSpan(span, origin) {
  span.setAttribute(SEMANTIC_ATTRIBUTE_SENTRY_ORIGIN, origin);
}

export { addOriginToSpan };//# sourceMappingURL=http://go/sourcemap/sourcemaps/3a67af7b780e0bfc8d32aefa96b8ff1cb8817f80/node_modules/@sentry/node-core/build/esm/utils/addOriginToSpan.js.map
